# AGENTS.md

このリポジトリは、ブラウザゲーム「派遣ギルド！」の開発用です。  
作業するAIエージェントは、以下のルールを必ず守ってください。

## 基本方針

- 大規模変更は禁止。
- 指示された範囲だけを最小差分で編集する。
- 仕様・世界観・UIを勝手に大きく変えない。
- 既存の雰囲気を維持する。
- このゲームは「戦うゲーム」ではなく、「派遣を眺め、帰還報告を読むゲーム」である。
- ログは大げさにしすぎない。
- 絶叫・派手な演出・ネオン系・サイバー系は禁止。
- 雰囲気は暗め、静か、少し不穏、ギルド報告書風。
- 羊皮紙、暗木、真鍮、古いTRPG寄りの質感を優先する。

## 作業前に必ず確認すること

作業前に必ず以下を確認してください。

```bash
git status --short
git status -sb
```

* 作業ツリーが clean でなければ編集しない。
* `[ahead 1]` など未pushのコミットが見える場合も編集しない。
* その場合は、状況を報告して作業を停止する。
* push はユーザーから明示されない限り禁止。

## 作業前に読むファイル

作業前に、必要に応じて以下を読むこと。

* `docs/operator_policy.md`
* `docs/status.md`
* `docs/equipment_battle.md`

エリア・敵・ストーリー関係の作業では、必要に応じて以下も読むこと。

* `docs/area_stage_plan.md`
* `docs/area_enemy_candidates.md`
* `docs/story_direction_memo.md`

## 文字コード・Markdown編集ルール

Markdownファイルを編集する場合は、特に注意すること。

* 既存MDはUTF-8のまま編集する。
* 文字コードを変えない。
* ファイル全体を再保存しない。
* 該当箇所だけを最小差分で更新する。
* Git diffで文字化け、CRLFによる全体差分、ファイル全体差し替えが見えた場合は、編集を中止して報告する。
* docs更新は、事実として実装済みになった内容だけを短く記録する。
* 未確認の仕様を確定事項として書かない。

## コード編集ルール

* `index.html` / `style.css` / `data.js` / `battle.js` / `game.js` の構造を尊重する。
* 既存関数の目的を確認してから編集する。
* 似た処理がある場合は、既存処理を流用する。
* 同じ機能を別実装で重複させない。
* 原因確認なしに大きく書き換えない。
* UI全面変更は禁止。
* セーブデータ互換を壊さない。
* 未来情報を画面に出さない。
* 派遣開始時に最終結果を表示しない。
* ログ進行に合わせてHP表示などを更新する。
* 死亡キャラは会話・行動しない。
* 冒険開始直後に死亡していないキャラ名を赤くしない。

## 戦闘・ログ方針

* 戦闘ログは淡く、報告書風にする。
* 毎回大げさな叫びや派手なセリフを出さない。
* 「アレンが前に出た」「アレンがミナをかばった」のような短い演出を基本にする。
* 「下がれ！」のような強いセリフは低頻度にする。
* ボス共通溜め攻撃は、明示されない限り復活させない。
* ボスの個性は、共通処理ではなく個別 `special` で出す。

## 装備・研究方針

* 装備研究は、主に記録を賑やかにするための要素。
* 研究が進むまで分解しない方が得、という設計にしない。
* 分解や売却まわりは、保護ロック・装備中・レア度・OP数の扱いに注意する。
* 装備保管庫や一括処理は、誤操作しにくいことを優先する。

## 報告ルール

作業後は、以下を短く報告する。

* 変更ファイル名
* 変更内容
* 確認したこと
* 未確認のこと
* pushしていないこと

pushはユーザーが明示した場合のみ行う。
